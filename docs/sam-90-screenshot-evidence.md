# SAM-90 Screenshot Evidence Validation

This short note documents that SAM-90 validated screenshot evidence capture from the Docker worker by building and running the toy webapp, then capturing its home page with `symphony-capture-url`.
